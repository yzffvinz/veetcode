/*
 * @lc app=leetcode.cn id=98 lang=javascript
 *
 * [98] 验证二叉搜索树
 *
 * https://leetcode-cn.com/problems/validate-binary-search-tree/description/
 *
 * algorithms
 * Medium (35.18%)
 * Likes:    1365
 * Dislikes: 0
 * Total Accepted:    395.6K
 * Total Submissions: 1.1M
 * Testcase Example:  '[2,1,3]'
 *
 * 给你一个二叉树的根节点 root ，判断其是否是一个有效的二叉搜索树。
 * 
 * 有效 二叉搜索树定义如下：
 * 
 * 
 * 节点的左子树只包含 小于 当前节点的数。
 * 节点的右子树只包含 大于 当前节点的数。
 * 所有左子树和右子树自身必须也是二叉搜索树。
 * 
 * 
 * 
 * 
 * 示例 1：
 * 
 * 
 * 输入：root = [2,1,3]
 * 输出：true
 * 
 * 
 * 示例 2：
 * 
 * 
 * 输入：root = [5,1,4,null,null,3,6]
 * 输出：false
 * 解释：根节点的值是 5 ，但是右子节点的值是 4 。
 * 
 * 
 * 
 * 
 * 提示：
 * 
 * 
 * 树中节点数目范围在[1, 10^4] 内
 * -2^31 <= Node.val <= 2^31 - 1
 * 
 * 
 */

// @lc code=start
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {boolean}
 */
var isValidBST = function(root, bot = -Number.MAX_SAFE_INTEGER, top = Number.MAX_SAFE_INTEGER) { // 对于祖先结点影响下的的取值范围，可概括为bot < x < top
    if (!root) return true;

    const {left, right, val} = root;

    if (val <= bot || val >= top) {
        return false;
    }

    const leftBot = bot;
    const leftTop = Math.min(val, top);
    const rightBot = Math.max(val, bot);
    const rightTop = top;

    const leftOk = (left === null) || isValidBST(left, leftBot, leftTop);
    const rightOk = (right === null) || isValidBST(right, rightBot, rightTop);

    return leftOk && rightOk;
};
// @lc code=end

